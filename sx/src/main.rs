use std::io::Write;

fn main() -> std::io::Result<()> {
    // let mut w = Vec::new();
    // //w实现了std::io::Write::write_fmt,所以下面能正常调用
    // write!(&mut w, "test\n")?;
    // write!(&mut w, "formatted {}", "arguments")?;
    // let v = String::from_utf8(w).unwrap();
    // println!("{}", v);
    // Ok(())
}
